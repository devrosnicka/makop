# Feature: <name>

> Copy this file to `docs/specs/<short-name>.md` and fill it in before starting
> implementation. Keep it small enough to implement and verify in one sitting;
> split into multiple specs if it's growing beyond that.

## Why
What problem or need does this address? What prompted it?

## User stories & acceptance criteria
- As a `<user>`, I want `<capability>`, so that `<benefit>`.
- [ ] Acceptance criterion 1 (concrete, checkable)
- [ ] Acceptance criterion 2

## Scope
**In scope:**
-

**Out of scope:**
-

## Technical approach
- Key files/modules touched:
- Existing utilities/patterns to reuse:
- Notable trade-offs or open questions:

## Verification
How we'll prove this works end-to-end (not just "tests pass"):
- [ ] Manual check / command to run:
- [ ] Renders correctly at a mobile viewport (~375px) — no horizontal overflow
- [ ] Automated tests covering:
